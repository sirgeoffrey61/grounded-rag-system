# Grounded RAG — consolidated dependencies (local, Docker, Render)
# Install: pip install -r requirements.txt

# Phase 1 — explore_dataset.py
tqdm>=4.66.0

# Phase 2 — ingest.py / Phase 6 — hybrid_retriever.py
rank-bm25>=0.2.2

# Phase 4–8 — qa_pipeline, grounded_qa, llm_client (Groq via OpenAI-compatible API)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
langchain-huggingface>=0.1.0
langchain-chroma>=0.2.0
langchain-openai>=0.2.0
langchain-ollama>=0.2.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
numpy>=1.24.0
matplotlib>=3.8.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Phase 10 — api/ (FastAPI backend)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
pydantic-settings>=2.0.0

# Phase 12 — frontend/app.py (Streamlit UI)
streamlit>=1.28.0
requests>=2.31.0

# Optional: only if TensorFlow is already installed in the same environment
# tf-keras>=2.16.0
